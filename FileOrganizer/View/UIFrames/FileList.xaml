<Page x:Class="FileOrganizer.View.UIFrames.FileList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FileOrganizer.View.UIFrames"
      mc:Ignorable="d" 
      d:DesignHeight="720" d:DesignWidth="300"
      Title="FileList">

    <Grid Background="#444444">
        <ListView ItemsSource="{Binding Source={StaticResource VModel}, Path=WorkingFiles}" HorizontalContentAlignment="Stretch" Background="#444444">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding Name}" VerticalAlignment="Center" HorizontalContentAlignment="Left"
                            Background="#222222" Foreground="#f5f5f8" Height="50" BorderThickness="0"
                            Command="{Binding Source={StaticResource VModel}, Path=FSSingleClick}" CommandParameter="{Binding Mode=OneWay}" MouseDoubleClick="DoubleClick"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
